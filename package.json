{
  "name": "autobrowser-monorepo",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter autobrowser-cli dev",
    "package:npm": "pnpm test && pnpm build && pnpm --filter autobrowser-cli run prepare:publish && node ./packages/cli/scripts/run-npm-release.mjs pack",
    "publish:npm": "pnpm test && pnpm build && pnpm --filter autobrowser-cli run prepare:publish && node ./packages/cli/scripts/run-npm-release.mjs publish",
    "test": "pnpm -r test",
    "typecheck": "pnpm build && pnpm -r typecheck"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "robotjs"
    ]
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "jsdom": "^29.1.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
