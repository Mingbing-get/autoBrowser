{
  "name": "autobrowser-monorepo",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @autobrowser/cli dev",
    "test": "pnpm -r test",
    "typecheck": "pnpm build && pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
